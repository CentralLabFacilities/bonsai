## Feature wish list

- Collapsable compound states
- Sourcing Support
- Import / Export as SCXML
- Save current graph layout
- Auto layout support (-> https://eclipse.dev/elk/)
- Condition support
- OnEntry/OnExit support
- Global data model support
- Slot representation (key as parameter or own graph layer?)
- If parameter, add autocompletion/dropdown for slot keys
- If parameter, add graph search function to find all nodes which use that specific slot
- Tooltips / Skill descriptions 
- Advanced skill selection with searchbar, sorted list etc.
- Highlighting of empty transitions
- Hide/show only success/error/fatal transitions
- If skill node in graph is selected, highlight all directly connected neighbors

## Long term TODOs

- Tidy up graph source code
- Make UI pretty, do some CSS magic
- Add support to run state machine directly out of graph view, highlight node of current state
